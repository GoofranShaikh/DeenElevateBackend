BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[TShirt] ADD [quantity] INT NOT NULL CONSTRAINT [TShirt_quantity_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
