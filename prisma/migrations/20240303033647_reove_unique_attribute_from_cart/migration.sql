BEGIN TRY

BEGIN TRAN;

-- DropIndex
ALTER TABLE [dbo].[Cart] DROP CONSTRAINT [Cart_customerId_key];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
